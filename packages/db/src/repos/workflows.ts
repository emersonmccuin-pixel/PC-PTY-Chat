// Scaffolded for the file-backed workflow registry sync. The runtime today
// loads YAMLs via @pc/workflows from the filesystem; this table stays empty
// until a follow-up sub-slice wires the sync hook.

// No exports yet — the table exists in schema for forward compat.
export {};
